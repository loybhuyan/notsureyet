document.getElementById('dropBeat').addEventListener('click', () => {
    const colors = ['#ff00ff', '#00ffff', '#39ff14', '#ffff00'];
    const randomColor = colors[Math.floor(Math.random() * colors.length)];
    
    document.querySelector('.psychedelic-bg').style.background = conic-gradient(from 0deg, , #000, );
    
    alert('?? Can you feel it? The rhythm is flowing through the code. You are the conductor of this digital symphony!');
});
